<UserControl x:Class="TBStyler.WPF.Views.AllView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TBStyler.WPF.Views"
             xmlns:viewmodel="clr-namespace:TBStyler.WPF.ViewModels"
             xmlns:view="clr-namespace:TBStyler.WPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        <view:StyleView DataContext="{Binding StyleSection}"/>
        <view:ColorPickerView DataContext="{Binding ColorPickerSection}"/>
        <view:AnimationView DataContext="{Binding AnimationSection}"/>
        <view:PositionView DataContext="{Binding PositionSection}"/>
        <view:ExtraView DataContext="{Binding ExtraSection}"/>
        <view:MechanicsView DataContext="{Binding MechanicsSection}"/>
    </StackPanel>
</UserControl>
